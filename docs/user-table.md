# ????????

## dots_users

???????????????? PlayerID ?????????????????????? `player_id`????????? `p1` / `p2`?

| ?? | ?? | ?? | ?? |
| --- | --- | --- | --- |
| `id` | bigint / string | ?? | ??????? |
| `openid` | varchar(64) | ????? | ????????????? |
| `player_id` | varchar(64) | ????? | ???????????? ID |
| `nickname` | varchar(24) | ?? | ??????????????? 12 ??? |
| `avatar_url` | varchar(255) | ?? | ?????? |
| `status` | varchar(16) | ????? `active` | ???? |
| `created_at` | datetime / number | ?? | ?????? |
| `updated_at` | datetime / number | ?? | ?????? |

?????
- `UNIQUE(openid)`
- `UNIQUE(player_id)`
- `INDEX(updated_at)`

?? `online-server/server.js` ???? Map ????????????????????????????????????????? `usersByOpenId` ? `openIdByPlayerId` ???????????????????

## ????

1. ????? `wx.login()` ???? code?
2. ????? `/api/users/login`?
3. ????????? openid ??????? `openid`?
4. ??????? openid??????? `WECHAT_APPID` ? `WECHAT_SECRET` ????? `jscode2session` ?? openid?
5. ???? openid ????????????? `player_id`?
6. ????? `{ openId, playerId, nickname }`?WebSocket `HELLO` ??? `playerId` ?????????
